# Simple Flask Web App

This is a simple Flask web app that displays "Hello, World!".

### How to run the app:

1. Install the required dependencies using `pip install flask`.
2. Run the app by executing `python app.py`.
3. Open your web browser and go to `http://127.0.0.1:5000/` to see the message "Hello, World!".